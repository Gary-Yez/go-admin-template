# 模板项目开发指引

本文件面向使用 go-admin-template 创建业务项目的开发者和 AI 编程助手，适用于整个业务仓库。目标是快速接入需求、优先复用框架、避免重复实现，并在每次交付后保持代码清晰。路径均相对于本项目，不依赖框架作者的本地维护环境。

## 开始任务：先获取最新依赖文档

1. 阅读本项目 README、本文件、相关业务代码及工作区差异，确认需求范围和现有实现。
2. 在涉及框架能力的每次新任务开始时，获取并阅读前后端最新线上 README，不能只依赖本文件、过往对话或旧版缓存：
   - 后端 Markdown：https://raw.githubusercontent.com/Gary-Yez/go-admin/master/README.md
   - 前端 Markdown：https://raw.githubusercontent.com/Gary-Yez/go-admin-web/master/README.md
   - 页面备用入口：[go-admin](https://github.com/Gary-Yez/go-admin#readme)、[go-admin-web](https://github.com/Gary-Yez/go-admin-web#readme)。Raw 访问失败时尝试页面中的 README 正文。
3. 从线上文档的“系统功能”确认是否已有能力，再从“公开 API / 导出与实际用法”选择包、方法、类型或组件，并阅读相关注意事项。必须读取正文，不能将搜索摘要当作已阅读文档。同一任务中已成功获取的文档可以复用，不反复请求。
4. 检查 `server/go.mod`、`web/package.json` 和锁文件中的实际版本。线上默认分支可能包含尚未发布或当前项目未安装的能力；新 API 必须核对当前安装版本是否支持。
5. 存在版本差异时，阅读对应 Tag 的 README 与实际依赖导出/源码。Go 可用 `go list -m -f '{{.Dir}}' github.com/Gary-Yez/go-admin` 定位实际源码；前端检查已安装包的 package.json、README 和 src/index.ts。不要为了照搬最新示例擅自升级依赖。
6. 联网失败时说明无法获取最新文档，暂用已安装版本 README 和源码，并标明验证范围；不能声称已读到最新文档或凭记忆编造接口。只有必须依赖缺失资料的工作才暂停，其余已核实工作可以继续。

## 项目职责与目录

本项目是业务宿主，后端依赖 `github.com/Gary-Yez/go-admin`，前端依赖 `@gary-yez/go-admin-web`。系统管理能力由依赖提供，业务仓库只维护业务扩展、入口和部署配置。

| 位置 | 职责 |
| --- | --- |
| `server/main.go` | 全局中间件与框架启动 |
| `server/modules/enter.go` | 业务模块注册 |
| `server/modules/<module>/enter.go` | 模块接口实现、路由、初始化及菜单 |
| 同模块 `model.go` | 持久化模型、输入结构和转换 |
| 同模块 `controller.go` | 参数绑定、身份读取、服务调用和响应 |
| 同模块 `service.go` | 业务规则、数据范围、查询、事务和缓存维护 |
| `server/settings/config.go` | 配置工具生成的类型与注册信息 |
| `server/settings/init.go` | 业务动态初始值 |
| `web/src/main.ts`、`pages.ts` | 应用入口与业务页面注册 |
| `web/src/apis/<module>.ts` | 模块 API 与接口类型 |
| `web/src/views/<module>/` | 页面、页面私有组件、类型与逻辑 |
| `web/src/components/`、`utils/`、`stores/` | 仅按实际需要创建的跨模块能力 |

具体启动、配置、安装和部署步骤以本项目 README 为准。独立业务项目不要求额外下载两个框架仓库，不修改 node_modules 或 Go 模块缓存，不把个人绝对路径或临时依赖替换写入可分发配置。

## 复用优先

- 实现前按“已有业务实现 → 最新依赖文档 → 当前版本公开导出/源码”查找；先说明准备复用的能力和必要的扩展，不直接从零设计第二套方案。
- 后端优先复用框架的模块注册、数据库、请求绑定、分页筛选排序、统一响应、身份、缓存锁、任务、配置和存储。具体名称、签名、错误语义从线上 README 获取，再按安装版本核对。
- 前端优先复用应用创建、页面注册、统一 request、Store、页面标题、表格、表单、删除确认、配置布局和文件上传组件。只从包公开入口导入，不深层导入内部页面、API 或运行容器。
- 不复制登录、权限、菜单、文件管理等系统模块；不另建数据库连接、Axios 实例、Pinia、路由或调度器来重复承担框架已有职责。确有独立第三方协议等需求时说明差异并隔离用途。
- 现有抽象不适用时，先明确缺口，再做满足当前需求的最小实现。不要增加仅转发参数的包装层，不为未来假设建立通用框架。
- CRUD 优先使用现有生成工具或参考当前生成模板；修改已生成的业务代码前检查定制内容，重新生成时先审阅覆盖差异。业务需求不默认改框架的通用生成器。

## 前后端开发约定

- 控制器负责 HTTP 输入输出，服务层负责业务规则。输入仅开放允许编辑的字段，数据归属和租户限制在后端实现，菜单隐藏不代替接口权限。
- 依赖初始化遵循框架生命周期：包 init 做注册，数据库、缓存、配置值及任务服务在框架就绪后使用；不重复初始化共享服务，不在运行中直接修改 Gin 路由。
- 多步写操作明确事务边界，事务内使用同一个 tx。处理错误、超时和取消；缓存锁不能替代数据库约束，多实例任务仍需幂等。
- 环境配置与业务配置分开，默认值不视为覆盖现有数据的迁移。生成文件遵守生成工具的维护方式，业务自定义逻辑放到可维护的扩展位置。
- 模块 API 放 `src/apis/<module>.ts`，页面私有代码留在 `src/views/<module>/`。同一模块的多页放在一起，TypeScript 导入不带 `.ts` 后缀。
- 沿用现有后台布局、公共组件、主题变量和交互，兼容明暗主题。列表与表单覆盖加载、空数据、失败、提交状态；不重复弹出统一请求层已处理的错误。
- 页面由动态映射和菜单标识连接，移动页面时同步引用。接口返回结构、组件属性、插槽与关闭行为按文档使用，不凭同名第三方组件猜测。
- 上传优先复用现有组件和文件 API，持久化文件 ID；关联时后端仍校验归属、完成状态和业务内容限制，不把临时 URL 当永久数据。
- 使用明确类型和清晰命名，减少不必要的 any、类型断言、重复状态及 watcher。函数按业务职责拆分，优先简单控制流与早返回。

## 每次完成或修正需求后必须清理

清理属于交付过程，范围是本次需求及其直接影响的代码；尊重用户已有修改，不顺手重构无关模块。

1. 删除本次新增或修改后失效的变量、导入、函数、参数、分支、组件、样式、旧接口和过时注释；不保留注释掉的旧实现、调试输出、废弃方案与无用途的 TODO。
2. 修正需求时替换旧逻辑，同步调用方、类型、路由、配置与文档，避免新旧方案并存；合并本次涉及的重复代码，移除无职责的包装层和无需求的兼容分支。
3. 删除前检查静态引用及动态入口：模块注册、空白导入、页面 glob、菜单组件字符串、反射标签、配置 Key、外部 API 契约。没有本地引用不等于无用。
4. 模板扩展入口、接口要求的空方法、生成标记和必要说明示例不能仅因暂时为空而删除。公开接口和历史迁移的移除需考虑兼容性。
5. 删除依赖前确认对应项目确实不再使用，再同步依赖清单与锁文件；不顺便升级依赖。代码清理不包含删除用户数据、存储对象或部署配置。
6. 检查函数职责、命名与控制流。注释解释原因和约束，不翻译代码；不为缩短行数写复杂表达式，不用空 catch 掩盖失败。
7. 没有可安全删除的冗余就保持原样，不为证明做过清理而制造改动。

## 验证与交付

- 按改动范围选择验证：Go 改动执行 gofmt、已有测试和必要的 go vet；前端执行类型检查，涉及打包或样式时执行构建。模板 web 的 `yarn build` 包含类型检查，server 可执行 `go test ./...`。
- 修复缺陷或增加业务规则时，按项目测试约定补充有价值的验证；避免只复述实现的测试、一次性脚本和无用途的产物。
- 接口、权限、数据库和上传变更验证对应行为与失败路径；编译成功不能代替真实环境验证。缺少环境时明确未验证的部分。
- 检查最终差异、新增文件和 `git diff --check`，确认没有调试残留、意外生成物、密钥或无关修改。
- 交付说明实际修改、复用的能力、清理内容、验证结果和限制，提供相关文件链接；不宣称未执行的检查通过。提交、推送和发布按用户明确要求执行。

## 文档维护约定

本项目 README 面向使用者，保持“项目介绍 → 系统功能 → 安装与引用 → 公开能力及实际用法 → 使用注意事项”的清晰结构，按需要补充架构、部署和排错。依赖的详细 API 说明由上游 README 维护，本文件只保留查阅方式和开发规则。

新增、修改或删除公开能力时，同步相应文档：说明导入路径、名称、参数/字段、默认值、返回值、错误行为、调用时机和实际示例；组件同时说明 props、事件、插槽与公开方法。不要把任务流水账、个人路径或未发布的设想当成已提供能力。发现文档与当前版本不一致时，以核实后的源码为准，并在本次交付中说明差异。
